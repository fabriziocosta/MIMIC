# Prescriptive Conclusions

- Use `latent_displacement` as the current strongest overall method in completed runs.
- `real_balanced` performs best around imbalance `2.0:1` in the `full` regime and is not indistinguishable from real balanced data in at least one completed comparison.
- `direct_smote` performs best around imbalance `2.0:1` in the `full` regime and is indistinguishable from real balanced data in at least one completed comparison.
- `direct_displacement` performs best around imbalance `2.0:1` in the `full` regime and is indistinguishable from real balanced data in at least one completed comparison.
- `latent_smote` performs best around imbalance `2.0:1` in the `full` regime and is indistinguishable from real balanced data in at least one completed comparison.
- `latent_displacement` performs best around imbalance `2.0:1` in the `full` regime and is indistinguishable from real balanced data in at least one completed comparison.
